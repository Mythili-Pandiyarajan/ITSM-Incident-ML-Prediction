# ITSM-Incident-ML-Prediction
ML system to predict incident priority, detect high-priority tickets, forecast volume &amp; flag RFC requests. XGBoost + Random Forest + Streamlit dashboard | IT Service Management
